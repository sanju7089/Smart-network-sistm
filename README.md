# Smart Work Network
## Separate-page navigation
Home → Services → Find Help → Work Details → Workers → Worker Profile → Checkout
Login → Role-based Dashboard
Worker: Dashboard → Work → Bookings → Earnings
Admin: Dashboard → Users → Verification → Jobs → Payments → Reports

## Production backend
The frontend demo uses browser localStorage. For a real public website, connect the `server/` API to a database and secure authentication/payment services.
